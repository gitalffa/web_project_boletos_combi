# Copia este archivo a ".env" (sin ".example") y llena tus datos reales.
# El archivo ".env" NUNCA se sube a Git (ver .gitignore).

DB_HOST=localhost
DB_USER=combi_app
DB_PASSWORD=escribe_aqui_tu_contraseña_real
DB_NAME=boletos_combi
PORT=3000
